import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';

import * as basic from './basic.stories';
import * as nested from './nested.stories';
import * as section from './section.stories';

<Meta title="Example/Card" />

# Card

页面内容区域布局卡片

## 基本使用

<Canvas
  of={basic.Basic}
  meta={basic}
></Canvas>

<Controls of={basic.Basic} />

## 嵌套使用

<Canvas
  of={nested.Nested}
  meta={nested}
></Canvas>

## 包含 Section

<Canvas
  of={section.Section}
  meta={section}
></Canvas>
