---
"@alauda/ui": patch
---

fix: configure v9 release workflow

Fix the `release/v9` release workflow so v9 stable releases publish with the `aui-v9` tag and v9 pull request prereleases publish with the `aui-v9-beta` tag.
